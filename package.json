{
  "name": "@gitforge/cli",
  "version": "0.0.1",
  "description": "Terminal-native management tool for GitForge",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "gitforge": "bin/gitforge"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "dependencies": {
    "@gitforge/sdk": "file:../typescript",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.10"
  }
}
